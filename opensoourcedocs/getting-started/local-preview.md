# Local Preview (Docs)

To preview these docs locally:

```bash
pip install -r requirements.txt
mkdocs serve
```

Then open `http://localhost:8000`.

Tip: This site is built from `opensoourcedocs/` (not `docs/`).
