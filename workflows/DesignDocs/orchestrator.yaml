workflow_name: DesignDocs
max_turns: 8
human_in_the_loop: false
startup_mode: AgentDriven
orchestration_pattern: DefaultPattern
initial_message_to_user: null
initial_message: 'DesignDocsAgent: concept_overview is provided via context variables
  (data_reference) for this app_id. Generate three Markdown documents (frontend, backend,
  database) using the normal AG2 LLM configuration (no direct HTTP clients). After
  each doc is generated, call save_design_doc with kind=frontend|backend|database,
  stage=draft, and the doc content. Finally output exactly: DONE.'
initial_agent: DesignDocsAgent
